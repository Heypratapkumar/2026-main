#include <stdio.h>

//    Calculator with All function //

// Use Comments "(/* */)" to Deactivate code part //

int main()
{

    // ADDITION //

    int a, b, sum;

    printf("Enter first number: ");
    scanf("%d", &a);

    printf("Enter second number: ");
    scanf("%d", &b);

    sum = a + b;

    printf("Sum = %d\n", sum);

    // SUBTRACTION  //

    /*
        int a, b, Diff;

        printf("Enter first number: ");
        scanf("%d", &a);

        printf("Enter second number: ");
        scanf("%d", &b);

        Diff = a-b;

        printf("Sum = %d\n", Diff);
        */

    // Multiplication. //
    /*
    int a, b, Product;

        printf("Enter first number: ");
        scanf("%d", &a);

        printf("Enter second number: ");
        scanf("%d", &b);

        Product = a*b;

        printf("Sum = %d\n", Product);
        */
    // DEVIDER//

    /*
    int a, b, Product;

        printf("Enter first number: ");
        scanf("%d", &a);

        printf("Enter second number: ");
        scanf("%d", &b);

        Product = a/b;

        printf("Sum = %d\n", Product);
        */
    // QUOTIENT AND REMAINDER //
    /*
        int a, b, quotient, remainder;

        printf("Enter dividend (first number): ");
        scanf("%d", &a);

        printf("Enter divisor (second number): ");
        scanf("%d", &b);

        quotient  = a / b;
        remainder = a % b;

        printf("Quotient  = %d\n", quotient);
        printf("Remainder = %d\n", remainder);
    */
    return 0;
}
